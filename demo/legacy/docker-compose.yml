services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: demo-legacy
    ports:
      - "${PORT:-8001}:80"
    volumes:
      - .:/var/www/html
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
    networks:
      - demo-network
    depends_on:
      - db
    restart: unless-stopped

  db:
    image: mysql:5.7
    container_name: demo-legacy-db
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_DATABASE:-legacy}
      MYSQL_USER: ${DB_USERNAME:-legacy}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - legacy-db-data:/var/lib/mysql
    networks:
      - demo-network
    restart: unless-stopped

volumes:
  legacy-db-data:

networks:
  demo-network:
    driver: bridge

