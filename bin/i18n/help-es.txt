Uso: %s [OPCIONES]

Genera comandos composer require a partir de "composer outdated --direct".
Funciona con cualquier proyecto PHP (Symfony, Laravel, Yii, CodeIgniter, CakePHP, Laminas, Slim, etc.)

OPCIONES:
    --run                    Ejecuta los comandos sugeridos automáticamente
    --release-info           Muestra información de releases (resumen con enlaces)
    --release-detail         Muestra el changelog completo de cada paquete (implica --release-info)
    --no-release-info        Omite la sección de información de releases (comportamiento por defecto)
    -v, --verbose            Muestra salida detallada (archivos de configuración, paquetes, etc.)
    --debug                  Muestra información de depuración (muy detallada, incluye rutas de archivos, parsing, etc.)
    -h, --help               Muestra este mensaje de ayuda

EJEMPLOS:
    %s                                    # Muestra comandos sugeridos (sin información de releases por defecto)
    %s --run                              # Ejecuta comandos sugeridos
    %s --release-info                     # Muestra resumen de información de releases
    %s --release-detail                   # Muestra changelogs completos (implica --release-info)
    %s --verbose                           # Muestra salida detallada con detalles de configuración
    %s --debug                             # Muestra información de depuración detallada
    %s --run --release-detail             # Ejecuta y muestra changelogs completos
    %s --verbose --release-info # Detallado con información de releases
    %s --debug                  # Modo depuración (muy detallado)

SOPORTE DE FRAMEWORKS:
    El script respeta automáticamente las restricciones de versión del framework:
    - Symfony: respeta la restricción "extra.symfony.require"
    - Laravel: respeta la versión major.minor de laravel/framework
    - Yii: respeta la versión major.minor de yiisoft/yii2
    - CakePHP: respeta la versión major.minor de cakephp/cakephp
    - Laminas: respeta las versiones major.minor de laminas/*
    - CodeIgniter: respeta la versión major.minor de codeigniter4/framework
    - Slim: respeta la versión major.minor de slim/slim

PAQUETES IGNORADOS:
    Los paquetes listados en generate-composer-require.yaml o .yml serán omitidos.
    El script busca archivos de configuración en el directorio actual (donde se encuentra composer.json).
    El formato antiguo (generate-composer-require.ignore.txt) todavía se admite por compatibilidad hacia atrás.
    Los comentarios que comienzan con # se ignoran.

INFORMACIÓN DE RELEASES:
    Por defecto, la información de releases NO se muestra (no se realizan llamadas a la API).
    Usa --release-info para mostrar un resumen con:
    - Nombre del paquete
    - URL del release
    - URL del changelog

    Usa --release-detail para ver el changelog completo del release.
    Usa --no-release-info para omitir explícitamente la información de releases (comportamiento por defecto).

CARACTERÍSTICAS DE RESOLUCIÓN DE CONFLICTOS:
    El script maneja automáticamente varios escenarios de conflictos de dependencias:

    1. Detección de Actualización de Paquetes Dependientes:
       - Cuando una actualización de paquete entra en conflicto con un paquete
         dependiente, el sistema verifica automáticamente si el paquete dependiente
         tiene una versión más nueva que soporte la actualización propuesta.
       - Si se encuentra, ambos paquetes se incluyen en el mismo comando de
         actualización.
       - Ejemplo: Si actualizar zircote/swagger-php:6.0.2 entra en conflicto con
         nelmio/api-doc-bundle que requiere ^5.0, el sistema verifica si
         nelmio/api-doc-bundle:6.0.0 existe y soporta ^6.0.

    2. Instalación Agrupada de Paquetes:
       - Cuando múltiples paquetes están filtrados por conflictos, el sistema
         sugiere instalarlos juntos en un solo comando.
       - A veces Composer puede resolver conflictos mejor cuando múltiples paquetes
         se instalan simultáneamente.
       - El sistema también incluye paquetes que pasaron la verificación de
         dependencias en comandos agrupados, ya que instalar todo junto puede
         ayudar a resolver conflictos.

    3. Detección de Paquetes Instalados Compatibles:
       - Al agrupar paquetes, el sistema verifica si los paquetes instalados
         necesitan actualizaciones para soportar los paquetes agrupados.
       - Si se encuentran versiones más nuevas compatibles, se incluyen
         automáticamente en el comando agrupado.

    4. Sugerencias de Contacto con Mantenedores:
       - Cuando no hay solución automática disponible, el sistema identifica
         paquetes que necesitan actualizaciones de sus mantenedores.
       - Proporciona información de contacto del mantenedor (email, URL del
         repositorio) y sugiere contactarlos para solicitar soporte para las
         versiones requeridas.

    5. Actualizaciones de Dependencias Transitivas:
       - Detecta automáticamente cuando las dependencias transitivas necesitan
         actualizaciones para resolver conflictos.
       - Sugiere actualizar estas dependencias primero, luego reintentar la
         actualización principal.

    6. Versiones de Respaldo:
       - Cuando la última versión causa conflictos, busca versiones anteriores
         compatibles que no tengan conflictos.

    7. Sugerencias de Paquetes Alternativos:
       - Para paquetes abandonados o cuando no existen versiones compatibles,
         sugiere paquetes alternativos con funcionalidad similar.

    8. Documentación de Casos Límite:
       - Cuando no hay solución automática disponible, proporciona explicaciones
         claras de por qué las actualizaciones están filtradas y qué se puede
         hacer al respecto.

CARACTERÍSTICAS DE SALIDA:
    - Los comandos se muestran con un icono 📋 para facilitar la copia
    - Comandos clickeables en terminales modernos (soporte de hyperlinks OSC 8)
    - Indicadores visuales claros para diferentes tipos de paquetes (prod/dev)
    - Información detallada de conflictos mostrando qué paquetes causan conflictos
    - Información de contacto del mantenedor cuando se requiere intervención manual
